---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import ContactForm from '../components/ContactForm.astro';
---

<BaseLayout 
  title="Contact" 
  description="Contactez Philippe Brel pour toute demande d'information, achat d'œuvre ou commande personnalisée."
>
  <Hero 
    title="Contact"
    subtitle="Restons en contact"
    size="small"
  />

  <section class="section contact-section">
    <div class="container">
      <div class="contact-layout">
        <!-- Formulaire -->
        <div class="contact-form-wrapper">
          <h2 class="contact-form-title">Envoyez-moi un message</h2>
          <p class="contact-form-intro">
            Remplissez le formulaire ci-dessous et je vous répondrai dans les plus brefs délais.
          </p>
          <ContactForm />
        </div>

        <!-- Informations -->
        <aside class="contact-info">
          <div class="contact-info-block">
            <h3>Contact Direct</h3>
            <p>Pour une réponse rapide, n'hésitez pas à me contacter directement :</p>
            
            <ul class="contact-list">
              <li>
                <a href="mailto:brelphilippe@gmail.com">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                    <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                    <polyline points="22,6 12,13 2,6"/>
                  </svg>
                  brelphilippe@gmail.com
                </a>
              </li>
              <li>
                <a href="tel:+33662459783">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                    <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
                  </svg>
                  +33 6 62 45 97 83
                </a>
              </li>
              <li class="contact-location">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
                  <circle cx="12" cy="10" r="3"/>
                </svg>
                France
              </li>
            </ul>
          </div>

          <div class="contact-info-block">
            <h3>Réseaux Sociaux</h3>
            <p>Suivez mon actualité et découvrez mes dernières créations :</p>
            
            <div class="contact-social">
              <a href="https://www.instagram.com/philippebrel/" target="_blank" rel="noopener" class="contact-social-link">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <rect x="2" y="2" width="20" height="20" rx="5" ry="5"/>
                  <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"/>
                  <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"/>
                </svg>
                Instagram
              </a>
              <a href="https://www.facebook.com/philippe.brel.9" target="_blank" rel="noopener" class="contact-social-link">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"/>
                </svg>
                Facebook
              </a>
              <a href="https://philippebrel.tumblr.com/" target="_blank" rel="noopener" class="contact-social-link">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <path d="M14.7 20a5 5 0 0 1-4.7-5V9H8V6a4 4 0 0 0 4-4h3v5h3v4h-3v5a1 1 0 0 0 1 1h2v3z"/>
                </svg>
                Tumblr
              </a>
            </div>
          </div>

          <div class="contact-info-block contact-info-block--highlight">
            <h3>Livraison</h3>
            <p>
              Livraison possible dans le monde entier sur demande. 
              Les frais de livraison sont calculés en fonction de la destination 
              et des dimensions de l'œuvre.
            </p>
            <p class="contact-worldwide">
              <span>🇫🇷</span> France
              <span>🇪🇺</span> Europe
              <span>🌍</span> Worldwide
            </p>
          </div>
        </aside>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .contact-section {
    padding-top: var(--space-xl);
  }
  
  .contact-layout {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: var(--space-2xl);
    align-items: start;
  }
  
  /* Form */
  .contact-form-wrapper {
    background: white;
    padding: var(--space-xl);
    box-shadow: var(--shadow-md);
  }
  
  .contact-form-title {
    font-size: 1.5rem;
    margin-bottom: var(--space-sm);
  }
  
  .contact-form-intro {
    color: var(--color-text-muted);
    margin-bottom: var(--space-lg);
  }
  
  /* Info blocks */
  .contact-info-block {
    margin-bottom: var(--space-xl);
    padding-bottom: var(--space-xl);
    border-bottom: 1px solid var(--color-border);
  }
  
  .contact-info-block:last-child {
    border-bottom: none;
    margin-bottom: 0;
    padding-bottom: 0;
  }
  
  .contact-info-block h3 {
    font-size: 1rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: var(--space-sm);
    color: var(--color-accent);
  }
  
  .contact-info-block > p {
    color: var(--color-text-muted);
    font-size: 0.9375rem;
    line-height: 1.7;
  }
  
  .contact-info-block--highlight {
    background: var(--color-bg-alt);
    padding: var(--space-lg);
    margin-left: calc(-1 * var(--space-lg));
    margin-right: calc(-1 * var(--space-lg));
    border-bottom: none;
  }
  
  /* Contact list */
  .contact-list {
    list-style: none;
    margin-top: var(--space-md);
  }
  
  .contact-list li {
    margin-bottom: var(--space-sm);
  }
  
  .contact-list a {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    color: var(--color-text);
    text-decoration: none;
    font-size: 1rem;
    transition: color var(--transition-fast);
  }
  
  .contact-list a:hover {
    color: var(--color-accent);
  }
  
  .contact-location {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    color: var(--color-text-muted);
    margin-top: var(--space-md);
  }
  
  /* Social */
  .contact-social {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-md);
    margin-top: var(--space-md);
  }
  
  .contact-social-link {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    color: var(--color-text);
    text-decoration: none;
    font-size: 0.9375rem;
    padding: var(--space-sm) var(--space-md);
    border: 1px solid var(--color-border);
    transition: all var(--transition-fast);
  }
  
  .contact-social-link:hover {
    border-color: var(--color-accent);
    color: var(--color-accent);
  }
  
  /* Worldwide */
  .contact-worldwide {
    display: flex;
    gap: var(--space-md);
    margin-top: var(--space-md);
    font-size: 0.9375rem;
  }
  
  .contact-worldwide span {
    margin-right: var(--space-xs);
  }
  
  /* Responsive */
  @media (max-width: 900px) {
    .contact-layout {
      grid-template-columns: 1fr;
    }
    
    .contact-info-block--highlight {
      margin-left: 0;
      margin-right: 0;
    }
  }
</style>

